# CNC CAM ツール マニュアル

**対象機械：Genmitsu 3018Pro（その他GRBLコントローラ採用機）**

**対象ソフト：VectorWorks / Illustrator / Affinity**

**推奨CNCコントローラー：CNCjs**

---

## 目次

1. [準備：DXFファイルの作り方](#1-準備dxfファイルの作り方)
2. [基本操作](#2-基本操作)
3. [パラメータ詳細](#3-パラメータ詳細)
4. [CNCjsでの実行手順](#4-cncjsでの実行手順)
5. [連続加工（量産）](#5-連続加工量産)
6. [外形切り抜き加工とタブ設定](#6-外形切り抜き加工とタブ設定)
7. [ネットワーク構成とCAMサーバー](#7-ネットワーク構成とcamサーバー)
8. [トラブルシューティング](#8-トラブルシューティング)

---

## 1. 準備：DXFファイルの作り方

### 1-1. レイヤー構成

DXFファイルには最低2つのレイヤーが必要です。

| レイヤー名 | 役割 | 描く図形 |
|---|---|---|
| `L1`（デフォルト） | 切削パス | LINE / LWPOLYLINE / SPLINE / CIRCLE |
| `ORIGEN`（デフォルト） | ワーク原点 | 円を1個だけ |

レイヤー名はツール上で自由に変更できます。大文字小文字は区別しません。

> ⚠️ **未設定レイヤーへの警告**：DXFに加工レイヤー・原点レイヤー以外のレイヤーにエンティティが存在する場合、解析ログに警告が表示されます。意図的に無視する場合はそのまま続行してください。

### 1-2. 原点の描き方

**原点レイヤーには円を1個だけ描いてください。**

この円の中心がCNC機械上の「刃先を合わせる点」になります。

```
原点の位置の考え方：

                    ↑ここに原点の円を置く（素材の右奥コーナーに刃先を当てる場合）
  ┌─────────────────●
  │                 │
  │   加工図形       │
  │                 │
  └─────────────────┘
```

**機械原点（右奥）をワーク原点として使う場合：**
加工図形の右上（右奥）コーナーに原点の円を置きます。図形全体は原点から左・手前方向（X-、Y-方向）に展開されます。

> ⚠️ **Affinityを使う場合**：DXF書き出し時に円がSPLINEに変換されます。ツールが自動的にSPLINEの重心を原点として検出しますが、正確な位置に円を配置してください。

### 1-3. ソフト別DXF書き出し設定

**VectorWorks**
- ファイル → 書き出し → DXF/DWG
- バージョン：R14（AC1014）推奨
- 単位：mm
- Illustratorを経由する必要はありません

**Illustrator**
- ファイル → 別名で保存 → DXF/DWG形式
- バージョン：R14推奨
- ⚠️ スケールが自動補正されます（$INSUNITSインチ問題）

**Affinity Designer / Publisher**
- ファイル → 書き出し → DXF
- ⚠️ 全パスがSPLINEに変換されます（自動対応済み）

---

## 2. 基本操作

### 2-1. ツールを開く

以下のURLをブラウザ（Chrome / Edge推奨）で開きます。

```
https://freezingdragon.github.io/CNC_web-CAM/cnc-cam.html
```

またはネットワーク内のCAMサーバー経由で開く場合：

```
http://cnc.local:8080/cnc-cam.html
```

> 💡 CAMサーバー経由で開くと、**▶ CNCjsへ** ボタンでGコードをCNC機械に直接転送できます。詳しくは [6. ネットワーク構成とCAMサーバー](#6-ネットワーク構成とcamサーバー) を参照してください。

### 2-2. DXFファイルの読み込み

1. 左パネル上部のドロップゾーン（「ドロップ または クリック」）にDXFファイルをドラッグ＆ドロップ
2. ファイル名とサイズが表示されれば読み込み成功

### 2-3. レイヤー設定

| 設定項目 | デフォルト値 | 説明 |
|---|---|---|
| 加工レイヤー | `L1` | 切削パスが描かれたレイヤー名 |
| 原点レイヤー | `ORIGEN` | 原点の円があるレイヤー名 |

DXFのレイヤー名と一致させてください。
DXFのレイヤー数より設定したレイヤー数が少ない場合は警告が表示されます。

### 2-4. 解析

**◈ 解析** ボタンをクリックします。

解析ログに以下が表示されれば正常です：

```
[L1] LINE×40             ← 加工レイヤーの内容
[ORIGEN] CIRCLE×1        ← 原点レイヤーの内容
✓ LINE: 40本
✓ 原点(CIRCLE r=1.23): (290.9, 200.6)
✓ BB: X(-285.9～-1.4) W=284.5mm
     Y(-191.1～-0.3) H=190.8mm
✓ 可動域チェック OK (295×400mm)
✓ チェーン結合: 40セグメント → 6チェーン
```

**エラーが出た場合は [7. トラブルシューティング](#7-トラブルシューティング) を参照してください。**

### 2-5. プレビューの見方

右側のキャンバスにツールパスが表示されます。

| 表示 | 意味 |
|---|---|
| 赤い点（右上） | 機械原点（X0 Y0） |
| 色分けされた線 | チェーン（同色 = Zが下がりっぱなしの連続切削） |
| 赤い○ | CIRCLE（G2円弧） |
| 黄色の点線 | バウンディングボックス（加工範囲の外形） |

**色が多いほどZの上下が多い（加工時間が長い）** ことを意味します。

### 2-6. トレースGコードの生成

**⬜ トレース** ボタンをクリックすると、加工範囲を空走確認するGコードが生成されます。

このGコードを先に実行して：
- クランプと刃先が干渉しないか
- 素材サイズに収まっているか

を目視で確認します。

`_trace.nc`ファイルをダウンロードしてCNCjsで実行してください。

### 2-7. 切削Gコードの生成

**⚙ Gコード生成** ボタンをクリックします。

Gコードの取得方法は2通りあります：

| 方法 | 手順 | 必要な環境 |
|---|---|---|
| ダウンロード | `_cut.nc`をダウンロード → CNCjsにアップロード | どこでも |
| 直接転送 | **▶ CNCjsへ** ボタンをクリック | CAMサーバー必要 |

---

## 3. パラメータ詳細

### 切削条件

| パラメータ | デフォルト | 推奨範囲 | 説明 |
|---|---|---|---|
| 総深さ | 2.0mm | 素材厚み以下 | 最終切削深さ |
| 切込み/パス | 0.5mm | 0.2〜1.0mm | 1パスの切り込み量。小さいほど刃への負荷が減る |
| 水平送り | 500mm/min | 300〜800mm/min | XY方向の切削速度 |
| Z進入送り | 150mm/min | 80〜200mm/min | プランジ速度。大きすぎるとZ軸が脱調する |

> ⚠️ **Z進入送りは控えめに設定してください。** Genmitsu系のZ軸は負荷が高く、速すぎると脱調して切削深さがずれます。150mm/min以下を推奨します。

### 安全設定

| パラメータ | デフォルト | 説明 |
|---|---|---|
| 移動退避高さ | 5.0mm | 線間の早送り退避高さ。クランプより高く設定 |
| 終了退避高さ | 30.0mm | 加工完了時の退避高さ。Z軸脱調対策で大きく設定 |
| スケール補正 | 1.0 | Illustratorのインチ問題は自動検出。通常変更不要 |
| SPLINE近似誤差 | 0.05mm | 小さいほど滑らか、Gコード行数が増える |

### スピンドル

| パラメータ | デフォルト | 説明 |
|---|---|---|
| 回転数（S値） | 10000 | GRBLの$30（最大RPM）に合わせる |
| ウォームアップ | 2.0秒 | スピンドル起動後の待機時間 |

### 機体可動域

| パラメータ | デフォルト | 説明 |
|---|---|---|
| X最大 | 280mm | 機械の実測X移動量に合わせる |
| Y最大 | 380mm | 機械の実測Y移動量に合わせる |

> Genmitsu 3040拡張キット使用時の実測値：X=295mm、Y=400mm

### ワーク配置オフセット

DXFの原点位置を基準に、加工図形全体をシフトします。

| 値 | 効果 |
|---|---|
| X+（正値） | 機械原点（右奥）方向にシフト |
| X-（負値） | 左方向にシフト |
| Y+（正値） | 奥方向にシフト |
| Y-（負値） | 手前方向にシフト |

### 接続設定

| パラメータ | デフォルト | 説明 |
|---|---|---|
| CAMサーバーURL（server.js） | `http://cnc.local:8080` | **▶ CNCjsへ** ボタンの送信先 |

CNC機械が複数台ある場合や、ホスト名・ポートが異なる場合はここを変更します。設定値はブラウザに記憶されます。

### 保持タブ（外形切り抜き専用）

外形切り抜き加工時に部材が飛散しないよう、素材の一部を切らずに残す「橋」です。詳しくは [6. 外形切り抜き加工とタブ設定](#6-外形切り抜き加工とタブ設定) を参照してください。

| パラメータ | デフォルト | 説明 |
|---|---|---|
| 保持タブを使う | オフ | チェックで有効化 |
| タブ数 | 4個 | 輪郭上に均等配置されるタブの数 |
| タブ幅 | 3.0mm | 1個のタブの長さ（刃径より大きく設定） |
| タブ高さ | 0.8mm | 底面から残す材料の高さ |

> ⚠️ タブ高さは総深さより必ず小さくしてください。大きい場合はエラーが表示されます。

---

## 4. CNCjsでの実行手順

### 4-1. 接続

1. ブラウザで `http://cnc.local:8000` を開く（Raspberry Piブリッジ使用時）
2. 接続パネルでPortを選択（`/dev/ttyUSB0`）
3. Baud Rate：115200
4. **Connect** をクリック

### 4-2. ホーミング

```
$H
```

をコンソールに入力して実行。機械が右奥のリミットスイッチに当たって戻ります。

> ⚠️ **$20=1（ソフトリミット有効）の場合、ホーミングをしないと加工できません。**

### 4-3. ワーク原点設定

1. CNCjsのジョグパネルで刃先を素材上の加工原点に移動
   - X方向：左右
   - Y方向：前後
   - Z方向：刃先を素材面に接触させる
2. コンソールに以下を入力：
   ```
   G10 L20 P1 X0 Y0 Z0
   ```
3. 座標表示がX:0 Y:0 Z:0になれば設定完了

### 4-4. Gコードのロード

**方法A：▶ CNCjsへ ボタンを使う（CAMサーバーあり）**

1. CAMツールで Gコード生成後、**▶ CNCjsへ** ボタンをクリック
2. `✓ 送信完了: xxx_cut.nc` が表示されたら成功
3. CNCjsの **Upload → 参照** からファイルを選択してロード

**方法B：ファイルをダウンロードしてアップロード（スタンドアロン）**

1. `_cut.nc`をダウンロード
2. CNCjsの **Upload** からファイルを選択

### 4-5. トレース実行（範囲確認）

1. `_trace.nc`をCNCjsにロード
2. **Play** で実行
3. 刃先が素材外形を空走するので、クランプとの干渉・素材サイズを目視確認
4. 問題なければ次へ

### 4-6. 切削実行

1. `_cut.nc`をCNCjsにロード
2. **Play** で実行
3. 加工完了後、自動的にZ+30mm、X0 Y0に復帰しスピンドルが停止

---

## 5. 連続加工（量産）

同じ図形を複数の素材に繰り返し加工する場合の手順です。

### 5-1. マクロの登録

CNCjsのマクロパネルに以下を登録します：

**マクロ名：次のワーク原点セット**
```gcode
G91
G0 Z-30.0
G90
G10 L20 P1 X0 Y0 Z0
```

### 5-2. 連続加工の手順

```
1回目の加工完了
  → 自動的にZ+30mm、X0 Y0に復帰

素材を交換・クランプ固定
  （XとYは原点位置に戻っているので再設定不要）

マクロ「次のワーク原点セット」を実行
  → Z軸が30mm下降して素材面をZ=0に設定

切削ファイルをPlay
  → 2回目の加工開始
```

> ⚠️ このマクロはZ軸を固定値（30mm）下げます。素材の厚みが変わる場合や、クランプ高さが変わる場合はジョグで手動Z合わせをしてください。

---

## 6. 外形切り抜き加工とタブ設定

### 6-1. 外形切り抜き加工とは

素材を完全に切り抜いて形を作る加工です。彫刻（表面に溝を掘る）と異なり、刃が素材を貫通します。

```
彫刻（線刻）         外形切り抜き
  ────────            ────────
  │      │            │      │  ← 素材
  │ ↓↓↓  │            │ ↓↓↓  │  ← 刃が貫通
  │      │            │      │
  ────────            ─┘    └─  ← 切り離される
  溝が残る             部品が分離
```

### 6-2. 外形切り抜き加工の注意点

**① 必ず捨て板を敷く**

刃が素材を貫通するとテーブル面を傷つけます。素材の下に安価な板（MDF端材など）を敷いてください。

**② 総深さは素材厚みより少し深く**

素材厚みが5mmの場合、総深さは `5.2mm` 程度に設定します。

**③ 部材が飛散する危険がある**

切り抜きが完了した瞬間に部材がスピンドルに弾かれることがあります。**保持タブを必ず使用してください。**

### 6-3. DXFの描き方（外形切り抜き）

外形線専用のレイヤーを準備します。
切り抜きたい形の輪郭を**閉じたパス**で描きます。

```
✅ 閉じたパス（正しい）    ❌ 開いたパス（タブ非対応）
  ┌──────┐                  ─────────
  │      │                           │
  │      │                  ─────────
  └──────┘
```

外形線を描いたレイヤで「保持タブを使う」のチェックを入れ、数値を適切に設定します。

> ⚠️ **工具径補正について**：刃の中心が輪郭線をなぞるため、実際の切り抜きサイズは輪郭線より刃径の半分（例：4mmエンドミルなら2mm）だけ小さくなります。正確なサイズが必要な場合は図面側でオフセットを加えてください（刃物径補正機能は将来実装予定）。

### 6-4. タブとは

タブとは切り抜き輪郭の一部を切らずに残す「橋」です。加工中に部材が飛散するのを防ぎます。
加工後はタブ部分をニッパー・ノミ・サンドペーパーで除去します。

### 6-5. タブの設定方法

ルール設定パネルの「保持タブを使う」チェックボックスをオンにします。

| 設定 | 推奨値 | 考え方 |
|---|---|---|
| タブ数 | 4〜6個 | 部材サイズに応じて。小さい部材は4個、大きければ増やす |
| タブ幅 | 刃径＋2mm以上 | 4mmエンドミルなら6mm以上。小さすぎると切り飛ばされる |
| タブ高さ | 0.5〜1.0mm | 高すぎると除去が大変。低すぎると強度不足 |

### 6-6. タブ設定の検証

解析ボタンを押すと、タブの妥当性チェックが実行されます：

| 表示 | 意味 |
|---|---|
| `✓ タブ設定OK` | 問題なし |
| `✗ タブ幅 ≤ 刃径 → タブ消失` | タブ幅を大きくしてください |
| `⚠ タブ残幅 Xmm（推奨: 刃径+2mm以上）` | タブ幅の増加を推奨 |
| `✗ タブ高さ ≥ 総深さ → タブ無効化` | タブ高さを総深さより小さくしてください |

### 6-7. 外形切り抜きの推奨設定値

**MDF外形切り抜き**

| 設定項目 | 推奨値 | 備考 |
|---|---|---|
| 総深さ | 素材厚み＋0.2mm | 捨て板に少し入る深さ |
| 切込み/パス | 0.5mm | 多パスで負荷を分散 |
| 水平送り | 400mm/min | 切り抜きは彫刻より遅めに |
| Z進入送り | 100mm/min | |
| タブ数 | 4個 | |
| タブ幅 | 6.0mm（4mmエンドミルの場合） | |
| タブ高さ | 0.8mm | |

**アクリル外形切り抜き**

| 設定項目 | 推奨値 | 備考 |
|---|---|---|
| 総深さ | 素材厚み＋0.2mm | |
| 切込み/パス | 0.3mm | アクリルは薄く少しずつ |
| 水平送り | 200mm/min | 熱による溶着防止 |
| Z進入送り | 80mm/min | |
| タブ数 | 4個 | |
| タブ幅 | 6.0mm | |
| タブ高さ | 0.5mm | アクリルは除去しやすいので薄くてよい |

> ⚠️ アクリルは送り速度が速すぎると摩擦熱で溶けて刃に絡まります。煙が出るようなら速度を下げてください。

---

## 7. ネットワーク構成とCAMサーバー

### 6-1. システム構成

CAMサーバー（server.js）をRaspberry Piに導入することで、ネットワーク内のどのPCからでもGコードをCNC機械に転送できます。

```
PC / Mac（ブラウザ）
  │
  ├── http://[ラズパイIP]:8080/cnc-cam.html  ← CAMツール
  │         ↕（▶ CNCjsへ ボタン）
  │   http://[ラズパイIP]:8080/save          ← Gコード転送
  │
  └── http://[ラズパイIP]:8000               ← CNCjs（機械操作）
            ↕ USB
        CNC機械
```

> 💡 **ポートの使い分け**
> - `:8000` → CNCjs（機械操作画面）
> - `:8080` → CAMサーバー・server.js（CAMツール配信＋ファイル転送）

### 6-2. CNC機械が複数台ある場合

ラズパイを台数分用意し、ホスト名を変えて設置します。

| 機械 | CNCjs | CAMサーバー |
|---|---|---|
| 1台目 | `http://cnc.local:8000` | `http://cnc.local:8080` |
| 2台目 | `http://cnc2.local:8000` | `http://cnc2.local:8080` |

CAMツールの **接続設定 → CAMサーバーURL（server.js）** を切り替えるだけで、どの機械にも送信できます。設定値はブラウザに記憶されます。

### 6-3. CAMサーバーなしで使う場合

server.jsが不要な場合（GitHub版など）でも、CAMツール本体は完全に動作します。

- DXF読み込み・解析・Gコード生成・ダウンロードは全て利用可能
- **▶ CNCjsへ** ボタンを押すと `✗ サーバーに接続できません` と表示されますが、他の機能への影響はありません
- 生成した`.nc`ファイルをCNCjsに手動でアップロードして使用してください

---

## 8. トラブルシューティング

### DXFを読み込んでもエンティティなしと表示される

**原因1：レイヤー名が違う**
ツールの「加工レイヤー」「原点レイヤー」の設定値と、DXFのレイヤー名が一致しているか確認してください。大文字小文字は区別しません。

**原因2：Affinityの行末コード問題**
通常は自動対応されます。もし発生する場合はブラウザをChrome/Edgeに変更してください。

---

### 原点レイヤーにCIRCLEなしと表示される

**VectorWorks / Illustratorの場合：**
原点レイヤーに円（CIRCLE）が1個あるか確認してください。複数ある場合はエラーになります。

**Affinityの場合：**
AffinityはDXF書き出し時に円をSPLINEに変換します。ツールが自動的にSPLINE重心を原点として検出するので、`⚠️ 原点にCIRCLEなし → SPLINE重心を使用`と表示されますが正常動作です。

---

### スケールがおかしい（図形が小さすぎる）

Illustratorから書き出したDXFに多い問題です。

解析ボタンを押すと`$INSUNITS`を自動検出してスケール補正欄に`25.4`が自動設定されます。設定されない場合は手動で`25.4`を入力してください。

---

### 可動域チェックエラーが出る

```
✗ X最小(-286.0) < -280
```

図形が機械の可動域を超えています。

**対処法1：機体可動域の設定を実測値に変更する**
`$$`コマンドで`$130`（X）`$131`（Y）の値を確認し、ツールの「X最大」「Y最大」に実測値を入力します。

**対処法2：オフセットで図形を移動する**
ワーク配置オフセットのX値を正の方向に動かして、可動域内に収めます。

**対処法3：図面側で原点位置を調整する**
原点の円を少し移動させると、図形全体がシフトされます。

---

### GRBLがアラーム状態になった

```
$X
```
でアラーム解除後、`$H`でホーミングしてください。

---

### 加工が終わってもスピンドルが止まらない

古いバージョンのGコードで発生した問題です。最新版のツールで生成したGコードは`M5`がフッターの先頭に配置されているため発生しません。古いGコードファイルが残っている場合は再生成してください。

---

### Z軸が深くなりすぎる

**原因1：Z合わせのズレ**
`G10 L20 P1 X0 Y0 Z0`を実行した時点で刃先が素材面より浮いていた場合、その分深くなります。Z合わせを慎重に行ってください。

**原因2：Z軸の脱調**
Z進入送り（feedZ）を下げてください。150mm/min以下を推奨します。また、スライダーのグリスアップも確認してください。

**原因3：テーブルのたわみ**
切削抵抗でテーブルがたわむと実切削深さが増えます。水平送り速度を下げるか、切込み量を減らしてください。

---

### ▶ CNCjsへ ボタンで「サーバーに接続できません」と表示される

**原因1：CAMサーバーURLが違う**
接続設定の「CAMサーバーURL（server.js）」が正しいか確認してください。CNCjsのURL（:8000）ではなく、server.jsのURL（:8080）を指定します。

**原因2：server.jsが起動していない**
ラズパイにSSH接続して確認します：
```bash
pm2 status
```
`cam`が`online`になっているか確認してください。停止している場合は：
```bash
pm2 start cam
```

**原因3：ローカルファイルから開いている**
`file://`で開いたcnc-cam.htmlからはネットワーク接続が制限される場合があります。`http://cnc.local:8080/cnc-cam.html`経由で開いてください。

---

### タブが効いていない（加工中に部材が動いた）

**原因1：開いたパスにタブを設定している**
タブは閉じたパス（輪郭が繋がっている）にのみ適用されます。DXFの輪郭パスが閉じているか確認してください。

**原因2：タブ幅が小さすぎる**
`✗ タブ幅 ≤ 刃径` のエラーが出ていた場合、タブが切り飛ばされています。タブ幅を刃径＋2mm以上に設定してください。

**原因3：タブ高さが低すぎる**
タブ高さが0.3mm以下だと加工中の振動で折れることがあります。0.5mm以上を推奨します。

---

### 切り抜いた部材のサイズが図面と違う

CAMツールは刃の中心が輪郭をなぞる設定です。実際の切り抜きサイズは輪郭線より刃径の半分だけ内側になります。

例：4mmエンドミル使用時、100×100mmの輪郭 → 仕上がり98×98mm

正確なサイズが必要な場合は図面側で輪郭を外側に刃径の半分（2mm）オフセットしてください。

---

### （CNCjsを使用する場合）3Dビジュアライザがフリーズした

加工自体は継続しています。以下を確認してください：

```bash
ssh pi@cnc.local
iwconfig wlan0
```

`Power Management:on`になっていたら：

```bash
sudo iwconfig wlan0 power off
```

で解消します。

---

## 付録：よく使うGRBLコマンド

| コマンド | 説明 |
|---|---|
| `$H` | ホーミング（全軸） |
| `$X` | アラーム解除 |
| `$$` | 全パラメータ表示 |
| `$I` | バージョン確認 |
| `?` | 現在ステータス確認 |
| `G10 L20 P1 X0 Y0 Z0` | ワーク原点設定 |
| `G90 G0 X0 Y0` | ワーク原点へ移動（XY） |
| `G91 G0 Z30.0` | Z軸を30mm上げる（相対移動） |
| `G90` | 絶対座標モードに戻す |

---

*[← READMEに戻る](README.md)*
